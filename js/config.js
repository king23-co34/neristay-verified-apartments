/* ===========================================================
   Neristay — runtime configuration
   -----------------------------------------------------------
   Central place to point the frontend at the live backend API.
   =========================================================== */
window.NERISTAY_CONFIG = {
  API_BASE_URL: 'https://backend-neriverified.onrender.com'
};
